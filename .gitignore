node_modules/
dist/
build/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/

# API build
apps/api/dist/

# Electron build artifacts (too large for GitHub)
apps/pos-desktop/dist/
apps/pos-desktop/dist-electron/
apps/pos-desktop/release/
*.dmg
*.zip
*.AppImage
*.exe
*.deb
*.rpm

# Shared types build — NO ignorar, Render lo necesita
# packages/shared-types/dist/

# Scratch and test files
apps/api/scratch/
*.fdb

# Binarios de Ollama bundleados (distribuidos vía DMG, no via git)
apps/pos-desktop/resources/ollama/
